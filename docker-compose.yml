services:
  streamer:
    build:
      context: .
      target: app
    env_file: .env
    environment:
      FX_PULSE_DB_PATH: /data/fx_pulse.db
    volumes:
      - ./data:/data
    restart: unless-stopped

  tests:
    build:
      context: .
      target: dev
    profiles: ["test"]
